#!/bin/bash

#spotifytui_node=$(bspc query -N -n)
[ -z $(pgrep -x spotify) ] && save_next_created_node_id spotify 
spotify_client_node=$(cat /tmp/last_node)
bspc node $spotify_client_node --flag hidden=on
setsid $TERMINAL -e spt 
wait
#bspc subscribe node_remove | while read EVENT
#do
    #notify-send "inside"
    #[ "${EVENT##* }" -eq "$spotifytui_node " ] && break
#done 
#bspc node "$spotify_client_node" -c
dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Pause
