#!/bin/bash

option="$(echo -e "suspend\nshutdown\nreboot\nexit" | dmenu -p "What do you want to do?" -i)"

case $option in
    suspend)
        lock_and_suspend
        ;;
    shutdown)
        echo xx
        ;;
    exit)
        bspc quit
        ;;
esac
        
